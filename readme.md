
# Arch Linux Dev Env Setup

```bash
yay -S osbuild-composer composer-cli

sudo systemctl enable --now osbuild-composer.socket
sudo systemctl enable --now osbuild-composer.service

composer-cli status show
```

# Building the Images

```bash


```
